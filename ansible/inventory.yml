all:
  hosts:
    local:
      ansible_host: localhost
    game_server:
      ansible_host: 192.168.0.50
      ansible_user: ansible

  children:
    # LXC containers
    lxcs:
      hosts:
        local:

    # QEMU VMs
    vms:
      hosts:
        game_server:

    # Docker machines
    docker:
      hosts:
        game_server:

    # Install portainer agent
    portainer_agent:
      hosts:
        game_server:
