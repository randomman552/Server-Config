all:
  hosts:
    config:
      ansible_host: localhost
    test-lxc:
      ansible_host: 192.168.0.99
    test-vm:
      ansible_host: 192.168.0.98
      ansible_user: ansible

  children:
    lxcs:
      hosts:
        config:
        test-lxc:

    vms:
      hosts:
        test-vm:

    docker:
      hosts:
        test-vm:
